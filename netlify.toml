[build]
  command   = "pnpm install && pnpm --filter @workspace/villah-portfolio run build"
  publish   = "artifacts/villah-portfolio/dist/public"

[build.environment]
  BASE_PATH = "/"
  PORT      = "3000"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
