{
  "buildCommand": "pnpm install && pnpm --filter @workspace/villah-portfolio run build",
  "outputDirectory": "artifacts/villah-portfolio/dist/public",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "env": {
    "BASE_PATH": "/",
    "PORT": "3000"
  }
}
